#include <stdio.h>

int main() {
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);

    char *factorial  = "1";
    char *i;
    for(i = 1; i <= num; i++) {
        
    }

    return 0;
}